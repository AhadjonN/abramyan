const a = 25;
const b = 15;
const c = Math.pow(a, 2) + Math.pow(b, 2);
const d = Math.pow(a, 2) - Math.pow(b, 2);
const i = Math.pow(a, 2) * Math.pow(b, 2);
const f = Math.pow(a, 2) / Math.pow(b, 2);
console.log(c, d, i, f);
